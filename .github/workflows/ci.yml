name: Optinetflow Node.js CI

on:
  push:
    branches:
      - main
      - dev

jobs:
  migration:
    runs-on: ubuntu-latest
    environment:
      name: ${{ startsWith(github.ref, 'refs/heads/main') && 'prod' || 'stage' }}
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: 'Create env file'
        run: echo "${{ vars.ENV_FILE }}" > .env

      - name: Install Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 18

      - uses: pnpm/action-setup@v2
        name: Install pnpm
        with:
          version: 8
          run_install: false

      - name: Get pnpm store directory
        shell: bash
        run: echo "STORE_PATH=$(pnpm store path --silent)" >> $GITHUB_ENV

      - name: Setup pnpm cache
        uses: actions/cache@v3
        with:
          path: ${{ env.STORE_PATH }}
          key: ${{ runner.os }}-pnpm-store-${{ hashFiles('**/pnpm-lock.yaml') }}
          restore-keys: ${{ runner.os }}-pnpm-store-
            

      - name: Install dependencies
        run: pnpm install

      - name: Cache pnpm modules
        uses: actions/cache@v3
        with:
          path: node_modules
          key: ${{ runner.os }}-pnpm-modules-${{ hashFiles('**/pnpm-lock.yaml') }}
      
      - name: Generate prisma client
        run: pnpm prisma generate

      - name: Migrate database
        env:
          DATABASE_URL: postgresql://${{ secrets.POSTGRES_USER }}:${{ secrets.POSTGRES_PASSWORD }}@localhost:1111/${{ secrets.POSTGRES_DB }}
        run: |
          sudo echo ${{ secrets.PRIVATE_KEY }} > ~/.ssh/id_rsa
          sudo chmod 600 ~/.ssh/id_rsa
        # run: |
        #   mkdir -p ~/.ssh
        #   echo ${{ secrets.PRIVATE_KEY }} > ~/.ssh/id_rsa
        #   chmod 600 ~/.ssh/id_rsa
        #   ssh -fN -L 1111:localhost:${secrets.DB_PORT} -o StrictHostKeyChecking=no ${secrets.SERVER_USERNAME}@${secrets.SERVER_IP} -p ${secrets.SERVER_PORT} 2>&1
        #   pnpm run migrate:deploy
  # build:
  #   runs-on: ubuntu-latest
  #   environment:
  #     name: ${{ startsWith(github.ref, 'refs/heads/main') && 'prod' || 'stage' }}
  #   steps:
  #     - name: Checkout
  #       uses: actions/checkout@v3

  #     - name: Create env file
  #       run: echo "${{ vars.ENV_FILE }}" > .env

  #     - name: Set env
  #       uses: c-py/action-dotenv-to-setenv@v2
  #       with:
  #         env-file: .env

  #     - name: Install Node.js
  #       uses: actions/setup-node@v3
  #       with:
  #         node-version: 18

  #     - uses: pnpm/action-setup@v2
  #       name: Install pnpm
  #       with:
  #         version: 8
  #         run_install: false

  #     - name: Get pnpm store directory
  #       shell: bash
  #       run: echo "STORE_PATH=$(pnpm store path --silent)" >> $GITHUB_ENV
          

  #     - name: Setup pnpm cache
  #       uses: actions/cache@v3
  #       with:
  #         path: ${{ env.STORE_PATH }}
  #         key: ${{ runner.os }}-pnpm-store-${{ hashFiles('**/pnpm-lock.yaml') }}
  #         restore-keys: ${{ runner.os }}-pnpm-store-

  #     - name: Install dependencies
  #       run: pnpm install

  #     - name: Cache pnpm modules
  #       uses: actions/cache@v3
  #       with:
  #         path: node_modules
  #         key: ${{ runner.os }}-pnpm-modules-${{ hashFiles('**/pnpm-lock.yaml') }}

  #     - run: pnpm run lint
  #     - run: pnpm run build
  #     - run: pnpm test
